# QRBOOKS — платформа QR-бронирования аудиторий

QRBOOKS — полнофункциональная система бронирования кабинетов колледжа. Решение включает веб-интерфейс для студентов и преподавателей, административную панель с расширенными возможностями и REST API. Бронирование может запускаться через веб или сканированием QR-кодов, а расписание кабинетов регулируется базовыми окнами доступности.

## Содержание

1. [Ключевые возможности](#ключевые-возможности)
2. [Архитектура и стек](#архитектура-и-стек)
3. [Подготовка окружения](#подготовка-окружения)
   - [Backend](#backend)
   - [База данных и миграции](#база-данных-и-миграции)
   - [Frontend](#frontend)
4. [Работа с административной панелью](#работа-с-административной-панелью)
5. [Механика базового расписания кабинета](#механика-базового-расписания-кабинета)
6. [Структура проекта](#структура-проекта)
7. [Учётные записи по умолчанию](#учётные-записи-по-умолчанию)
8. [Рабочий процесс разработчика](#рабочий-процесс-разработчика)
9. [Полезные команды](#полезные-команды)
10. [Отладка и устранение неполадок](#отладка-и-устранение-неполадок)
11. [Безопасность](#безопасность)

---

## Ключевые возможности

- **Роли и аутентификация**: студенты и преподаватели регистрируются самостоятельно, администраторы создаются из панели. Авторизация на JWT в httpOnly cookies, refresh-токен, контроль лимитов.
- **Умное расписание**: 
  - расчёт свободных окон на 24 часа вперёд, исключая пересечения;
  - ограничение бронирований внутри базового рабочего промежутка (например, 08:00–20:00) для каждого кабинета;
  - динамическая валидация «не в прошлом», проверка конфликтов и ручной перенос.
- **QR-инфраструктура**: генерация PNG-кодов для кабинетов, доступных для скачивания из админки.
- **Административная панель нового поколения**:
  - вкладка «Обзор» со сводной статистикой (кабинеты, брони, пользователи);
  - вкладка «Кабинеты» с фильтрами, массовой блокировкой, настройкой окна доступности, просмотром истории, мгновенным созданием броней и обновлением QR;
  - вкладка «Брони» с поиском, фильтрами по датам, массовой отменой и онлайн-редактированием брони (время, статус, пользователь);
  - вкладка «Пользователи» для управления профилями, ролями и сбросом пароля;
  - вкладка «Журнал» — аудит действий пользователей.
- **Статистика и отчёты**: расширенный дашборд в админке (загрузка аудиторий, распределение броней по статусам, распределение пользователей по ролям).
- **API-клиент**: типизированный React-клиент (`frontend/src/api/client.ts`) с удобными хелперами для всех административных сценариев, включая массовые операции и фильтрацию.

## Архитектура и стек

| Слой      | Технологии и примечания |
|-----------|-------------------------|
| Backend   | Python 3.12, Flask, SQLAlchemy 2.x, Alembic, Flask-JWT-Extended, Flask-Limiter |
| Frontend  | React 18, TypeScript, Vite, CSS-модули (см. `src/styles/global.css`) |
| База данных | PostgreSQL 15 (локальная и боевая среды) |
| Утилиты   | npm scripts, seed-скрипт, QR генератор (qrcode) |

**Сервисный слой** в `backend/app/services/` инкапсулирует бизнес-логику: проверка пересечений, почасовые ограничения, массовые операции.

## Подготовка окружения

### Backend

```bash
cd backend
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
# если пакет psycopg не собирается, установите системные зависимости из README PostgreSQL или используйте `pip install psycopg[binary]`
cp .env.example .env
```

> ⚠️ Обязательно укажите `DATABASE_URL` в `.env` с подключением к PostgreSQL, например `postgresql+psycopg://qrbooks:qrbooks@localhost:5432/qrbooks_dev`. Приложение не стартует без доступной БД.

### База данных и миграции

1. **Создайте базу данных PostgreSQL (однократно)**
   ```bash
   createdb qrbooks_dev
   # или psql -c "CREATE DATABASE qrbooks_dev;"
   ```
   При необходимости добавьте пользователя и пароль, чтобы совпадали с `DATABASE_URL`.

2. **Инициализация Alembic (однократно)**
   ```bash
   flask --app app db init
   ```
   Если директория `migrations/` уже существует, убедитесь, что в корне есть файл шаблона `migrations/script.py.mako`. Стандартное содержимое:
   ```mako
   """${message}

   Revision ID: ${up_revision}
   Revises: ${down_revision|comma,n}
   Create Date: ${create_date}

   """

   from alembic import op
   import sqlalchemy as sa
   ${imports if imports else ""}

   revision = '${up_revision}'
   down_revision = ${down_revision|repr}
   branch_labels = ${branch_labels|repr}
   depends_on = ${depends_on|repr}

   def upgrade():
       ${upgrades if upgrades else "pass"}

   def downgrade():
       ${downgrades if downgrades else "pass"}
   ```

3. **Применение миграций**
   ```bash
   flask --app app db upgrade
   ```

4. **Создание новой миграции**
   ```bash
   flask --app app db migrate -m "short description"
   flask --app app db upgrade
   ```

5. **Первичное заполнение** (создание ролей, учебных кабинетов, тестовых пользователей):
   ```bash
   python seed.py
   ```

### Frontend

```bash
cd ../frontend
cp .env.example .env  # при необходимости настройте API_BASE_URL
npm install          # Node.js >= 18 (рекомендуется 20 LTS)
npm run dev
```

Приложение откроется на `http://localhost:5173`. Прокси настроен на backend `http://localhost:5000`.

## Работа с административной панелью

Войдите под администратором (см. [учётные записи](#учётные-записи-по-умолчанию)). Панель включает пять вкладок:

1. **Обзор** — общая статистика (кабинеты, брони, пользователи) + таймстемп последнего обновления.
2. **Кабинеты**:
   - фильтры по названию, типу, статусу и блокировке;
   - кнопки массовой блокировки/разблокировки (выделите чекбоксами);
   - создание нового кабинета с указанием типа и базового окна (начало/конец);
   - настройка окна доступности для существующего кабинета (`Настроить время`);
   - создание брони в выбранный кабинет без перехода в другой раздел;
   - просмотр истории (все прошлые брони с датами и статусами) и обновление QR-кода.
3. **Брони**:
   - поиск по кабинету/пользователю, фильтры по статусам и временным диапазонам;
   - inline-редактирование (дата/время, статус, смена пользователя);
   - массовая отмена выбранных броней.
4. **Пользователи**:
   - фильтр по ролям, поиск по имени;
   - изменение имени, ролей, сброс пароля.
5. **Журнал** — истории действий (создание броней, отмена, логины, админские операции) с отметкой времени.

## Механика базового расписания кабинета

Каждый кабинет может иметь «окно доступности» — временной диапазон внутри суток:

- Поля `booking_start` и `booking_end` задаются администратором (например, `08:00`–`20:00`).
- Бронь создаётся/редактируется только внутри этого окна. Попытки выйти за границы приводят к ошибке.
- Автогенерация свободных окон сдвигает доступный интервал в пределах окна и исключает пересекающиеся брони.
- Если окно не задано, кабинет доступен круглосуточно.

## Структура проекта

```
backend/
  app/
    routes/          # Flask blueprints (auth/rooms/reservations/admin)
    services/        # Логика: проверки, фильтры, массовые операции
    utils/           # QR, время, пагинация, безопасность
    models.py        # SQLAlchemy модели и enum-ы (RoomType, ReservationStatus, UserRole)
    security.py      # JWT, лимиты, декораторы ролей
  migrations/        # Alembic миграции (инициализировано)
  seed.py            # Скрипт первичного наполнения данных
frontend/
  src/
    api/             # Типизированный REST-клиент (fetch)
    pages/           # Маршруты (Home, RoomDetail, Dashboard, Admin, Login, Register)
    components/      # Общие элементы UI
    styles/          # Глобальные стили и кастомные классы
README.md
```

## Учётные записи по умолчанию

| Роль       | Логин   | Пароль       |
|------------|---------|--------------|
| admin      | admin   | admin1234    |
| teacher    | teacher | teacher1234  |
| student    | student | student1234  |

> После запуска seed-скрипта эти записи доступны в локальной базе. В боевом окружении рекомендуется сменить пароли сразу после деплоя.

## Рабочий процесс разработчика

1. Создайте ветку, внесите изменения.
2. Примените `flask db migrate` / `upgrade` при изменении схемы.
3. Запустите локально backend (`flask --app app run`) и frontend (`npm run dev`).
4. Прогоните линтеры: `npm run lint`, `ruff check`/`flake8` (при наличии конфигурации).
5. Обновите `README.md` или `.env.example`, если менялись параметры запуска.
6. Сделайте commit и создайте pull request с описанием изменений и инструкциями по проверке.

## Полезные команды

```bash
# Backend
flask --app app run --host=0.0.0.0 --port=5000
flask --app app db migrate -m "message"
flask --app app db upgrade
python seed.py

# Frontend
npm run dev
npm run build
npm run lint
```

## Отладка и устранение неполадок

| Проблема | Решение |
|----------|---------|
| `flask db migrate` жалуется на `migrations/script.py.mako` | Убедитесь, что файл шаблона существует (см. [База данных и миграции](#база-данных-и-миграции)). |
| Ошибка `Не удалось подключиться к БД ...` при старте | Проверьте `DATABASE_URL` в `.env` и убедитесь, что PostgreSQL запущен и принимает коннекты. |
| `npm run dev` не видит backend | Проверьте `frontend/.env` — переменная `VITE_API_BASE_URL` должна указывать на `http://localhost:5000`. |
| Бронь не создаётся, сообщение «Reservation starts before allowed time» | Проверьте базовое окно аудитории в админке (вкладка «Кабинеты» → «Настроить время»). |

## Безопасность

- Пароли хранятся в bcrypt (`flask-bcrypt`).
- Роли проверяются декоратором `@role_required`, админ-действия доступны только `admin`.
- Rate limiting (`Flask-Limiter`) защищает чувствительные эндпоинты (логин, создание брони, админ маршруты).
- JWT и refresh-токены передаются в httpOnly cookie; фронт получает только данные пользователя.
- SQLAlchemy ORM предотвращает SQL-инъекции; дополнительная валидация дат/окон реализована в сервисах.

---

Проект активно развивается: ближайшие задачи — уведомления перед началом брони, интеграция с календарём и тестовое покрытие сервисов. Вкладывайтесь через PR или Issue! 👋
